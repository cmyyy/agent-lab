# PRD：agent-lab — 双实现 Agent 实验室

> 状态：草稿（待评审）
> 版本：v0.1
> 关联文档：`README.md`、`codex-project/docs/requirements/feature-search-knowledge-rag.md`
> 目标读者：项目作者（开发者本人）、后续接入的 Codex 执行会话

## Problem Statement（问题陈述）

项目作者正在准备 Agent 方向求职，需要一份能同时证明「理解底层机制」与「会用主流框架」的作品。当前 `agent-lab` 只有手写版雏形，存在四个缺口：

1. 知识检索是硬编码关键词匹配：知识库写死在代码里，无法动态扩展，语义相近的提问无法命中，难以作为「工程能力」展示点；
2. LangChain 版尚未建立，「同一套工具两套实现」的对比叙事不成立；
3. 没有可观测性（每次 LLM 调用与工具调用的成本、延迟、成败）与 golden 回归，两版差异无法量化，改动影响无法客观评估；
4. eval 只有结构断言雏形，尚未达到可支撑「重构不改语义」的回归基线。

## Solution（解决方案）

把 `agent-lab` 建成一个共享工具、双实现、可验证的 Agent 实验室：

- 同一套 6 个工具与 schema，通过单一工具注册中心暴露给两套实现（手写版 / LangChain 版）；
- 工具层优先升级 RAG：知识库从目录动态加载、语义检索、结果带来源与相似度，成为可演示、可扩展的深模块；
- 建立统一 eval 回归：结构断言 + golden 用例 + 三向对比（手写 vs LangChain vs Hermes 设计溯源）；
- 后续补齐可观测性（SQLite 记录 LLM 调用、工具调用、eval 运行），量化两版差异。

验收以「可观察行为」为准：`python evals/verify_core.py` 全绿、三个演示场景跑通、新增知识库文档无需改代码即可命中。

## User Stories（用户故事）

1. 作为开发者，我想要一个共享工具注册中心，以便手写版与 LangChain 版使用完全相同的工具定义，对比叙事成立。
2. 作为开发者，我想用纯 OpenAI SDK 跑通 AgentLoop（tool_call → 分发 → 回填 → 跳出），以便向面试官讲清底层机制。
3. 作为开发者，我想用 LangChain / LangGraph 实现同一套工具的版本，以便展示主流框架能力并对比架构取舍。
4. 作为开发者，我想对知识库做语义检索（RAG），以便演示「动态知识库 + 语义命中」而非关键词匹配。
5. 作为开发者，我想往知识库目录新增 Markdown 文档后无需改代码即可被检索，以便知识库可持续扩展。
6. 作为开发者，我想工具依赖不可用时（如 embedding 服务未配置）该工具自动从 schema 中隐藏，以便模型不会幻觉调用。
7. 作为开发者，我想所有工具永不抛异常、错误以结构化 dict 返回，以便模型能依据错误自愈。
8. 作为评估者，我想一条命令跑完回归 eval，以便任何改动后快速确认核心行为未漂移。
9. 作为评估者，我想拥有 golden 回归（query → 期望工具 / 期望字段），以便量化两版实现与每次改动的行为差异。
10. 作为开发者，我想记录每次 LLM 调用与工具调用的元数据（模型、token、延迟、成败），以便对比两版的成本与稳定性。
11. 作为面试官，我想看到「手写版 vs LangChain 版 vs Hermes」的三向对比文档，以便判断候选人的理解深度。
12. 作为开发者，我想项目可离线 / 低成本运行（mock 客户端、本地向量检索、国内可直连的 embedding 服务），以便快速迭代。
13. 作为开发者，我想 eval 只断言外部行为而非实现细节，以便重构时测试语义不失效。
14. 作为开发者，我想每次改动后三个演示场景（多工具链、搜索 + 保存笔记、错误自愈）仍能跑通，以便人工冒烟成本最低。
15. 作为开发者，我想新增工具只需在共享层注册（schema + handler），无需改动 AgentLoop，以便工具扩展不影响编排。
16. 作为开发者，我想检索结果带来源与相似度，以便模型回答时可引用、调试时可定位。
17. 作为评估者，我想删除知识库目录或禁用 embedding 时工具优雅降级，以便故障场景也可被测试覆盖。

## Implementation Decisions（实现决策）

### 模块划分

- **ToolRegistry（已有，保持为深模块）**：唯一工具入口，暴露 `get_definitions()`（按可用性过滤）与 `dispatch()`（永不抛异常）。两版实现只依赖它，接口保持稳定。
- **RAG Retriever（新建，深模块候选）**：对外只暴露 `search(query, top_k) → results` 一个稳定接口；内部封装文档加载、文本切块、embedding、向量检索。不把切块 / 检索细节泄漏给工具层。
- **Embedding 后端（可插拔）**：优先 SiliconFlow（`BAAI/bge-large-zh-v1.5`，国内直连、免费额度），提供本地纯 numpy 手写向量检索作为离线降级；后端可用性接入 `check_fn`——不可用时 `search_knowledge` 自动不暴露。
- **AgentLoop（保持薄编排）**：只做「调 LLM → 分发 → 回填 → 跳出」循环，LLM 客户端可注入（测试用 fake client），不承载业务逻辑。
- **LangChain 实现线（新建）**：复用同一注册中心与同一工具实现，仅替换循环编排；与手写版共享全部 eval。
- **Observability store（后续里程碑）**：SQLite 三张表（LLM 调用、工具调用、eval 运行），作为两版对比的数据底座。

### 接口契约

- `search_knowledge(query)` 签名与返回结构保持兼容：`{"results": [...]}`；升级后每个结果项包含文本、来源（文件名 + 块序号）、相似度。
- 工具错误契约不变：任何失败返回 `{"error": "..."}`，不抛异常。
- 工具 schema 的 description 随升级更新为「语义检索」，避免模型按关键词搜索理解。
- 知识库语料约定：目录加载、UTF-8、按空行分段、超长段再按长度切，中文不切碎语义。

### 架构决策

- RAG 优先于 LangChain 版实施：先用一个深模块打通「可演示价值」，再复制实现线。
- 不引入重型向量数据库（生产级 Chroma 部署等）：个人项目用纯 Python 方案，避免环境摩擦。
- 设计溯源保持为项目惯例：每个模块标注对照的 Hermes 源码位置，作为面试讲解材料。

## Testing Decisions（测试决策）

- **好测试的标准**：只断言外部行为——返回值的形状与内容、工具是否暴露、循环轮数、故障降级行为；不断言内部变量、私有方法或实现顺序。
- **被测模块**：
  - 工具注册与分发（shared_tools）：schema 一致性、dispatch 三路径（正常 / 未知 / 注入）、`check_fn` 过滤；
  - RAG Retriever：语义命中（不依赖关键词）、未命中返回空、动态语料（新增文档即可检索）、故障降级（目录缺失 / embedding 不可用）；
  - AgentLoop：注入 fake client，断言「分发 + 跳出」的轮数与最终回答；
  - LangChain 版：复用同一组行为断言，实现跨版本一致性验证。
- **先例**：`evals/verify_core.py` 已用 fake client + 结构断言覆盖注册表与循环，作为本 PRD 的回归基座；后续按 golden 格式（`{query, expected_tools, expected_fields}`）扩展。

## Out of Scope（范围外）

- 真实天气 / 时区 API 接入（`get_weather`、`get_time` 升级为独立需求）。
- Web 前端、聊天界面、多模态与语音。
- 重型向量数据库的生产化部署与运维。
- Spring AI / Java 实现线（属于求职规划的另一条线，不进本仓库）。
- Hermes 全量能力复制：多 Agent 协作、插件动态发现、20+ 平台网关等。

## Further Notes（其他说明）

- 项目定位是「面试作品 + 学习闭环」，因此代码注释与文档会保留设计溯源（对照 Hermes 源码位置），这是刻意的产品决策。
- 人工冒烟保持 3 个演示场景（多工具链、搜索 + 保存笔记、错误自愈），作为自动化测试之外的最终验收。
- 本 PRD 先行落盘在仓库根目录；agent-lab 尚未配置 git 远端与 issue tracker，待 GitHub 仓库建立后可同步发布并打上 triage 标签。
